name: Auto Merge Clash YAML

on:
  push:
    paths:
      - clash.txt
  workflow_dispatch:

permissions:
  contents: write

jobs:
  merge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install yq
        run: |
          sudo wget https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 -O /usr/local/bin/yq
          sudo chmod +x /usr/local/bin/yq

      - name: Create temp folder
        run: mkdir sources

      - name: Read YAML content from URLs (skip failures & clean URLs)
        run: |
          index=1
          while read url; do
            # 去掉前后空格
            url=$(echo "$url" | xargs)
            [ -z "$url" ] && continue

            # 检查是否以 http/https 开头
            if [[ "$url" != http* ]]; then
              echo "Invalid URL format: $url — skipped"
              continue
            fi

            echo "Fetching $url"
            if content=$(curl -fsSL "$url"); then
              filename="sources/$index.yaml"
              echo "$content" > "$filename"
              echo "Loaded into $filename"
              index=$((index + 1))
            else
              echo " Failed to fetch $url — skipped"
            fi
          done < clash.txt

      - name: Check if any YAML loaded
        run: |
          if ! ls sources/*.yaml >/dev/null 2>&1; then
            echo "No valid YAML content loaded — stopping workflow"
            exit 0
          fi

      - name: Add sequence numbers to proxies.name
        run: |
          i=1
          for file in sources/*.yaml; do
            count=$(yq eval '.proxies | length' "$file" 2>/dev/null || echo 0)
            if [ "$c]()
